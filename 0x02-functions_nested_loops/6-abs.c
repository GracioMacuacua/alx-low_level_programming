#include "main.h"

/**
 * _abs - Computes the absolute value of a integer
 * @n: The value to be transformed
 * Return: The absolute value of the number
 */
int _abs(int n)
{
	return (n >= 0 ? n : -n);
}